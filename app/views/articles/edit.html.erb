<div class="container">
  <h1><%= t('article.edit_article_title') =%></h1>

  <%= render "form", article: @article, class: "your-class-name" %>

  <div class="d-flex gap-2">
    <%= link_to  t('article.buttons.show'), @article, class:"btn btn-secondary" %>
    <%= link_to t('article.buttons.back_to'), articles_path, class:"btn btn-secondary" %>
  </div>
</div>